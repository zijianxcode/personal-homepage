# 安全整改记录 - 2026-03-19

## 本次检查结论

本项目在 2026-03-18 ~ 2026-03-19 进行了一轮完整安全检查与整改。检查重点包括：

- 后台入口与管理员鉴权
- 访客私信读取权限模型
- 受限内容（Things）访问控制
- 第三方脚本执行面
- 访客统计能力替代方案
- 发布基线与多端同步流程

## 发现的问题

1. 存在把访问码、入口 key、受限内容链接放在前端/仓库中的情况。
2. 管理后台原设计缺少服务端短期会话、登录限流与失败延迟。
3. 访客私信读取曾依赖前端可伪造标识，权限边界不够稳固。
4. 公共页面存在第三方脚本执行面，供应链风险未被显式控制。
5. 之前发布更偏向“功能是否可用”，对“安全模型是否真实成立”重视不够。

## 已完成整改

- 管理后台改为服务端签发短期会话 token。
- 增加管理员登录限流、失败延迟与 `Authorization` 鉴权。
- 访客私信改为服务端 visitor session，不再接受前端自定义读取凭证。
- Things 受限内容改为服务端校验许可码后返回，真实链接移出前端与仓库追踪内容。
- 首页与研究页的第三方统计恢复为兼容展示，同时接入 CloudBase 自建 UV 影子统计。
- `floating-clock` 改为本地 vendored 依赖，不再执行外部 CDN 脚本。
- 本地静态服务补充基础安全响应头。
- 构建脚本扩展为完整复制整站静态资源，确保安全修复不会遗漏到发布产物。

## 根因复盘

### 为什么之前没有第一时间注意到

- 关注点偏向视觉、交互和页面可用性，对“权限边界是否真实生效”缺少专门审查。
- 把“入口藏起来”误当成了一部分安全措施，而不是只当作 UI 层遮挡。
- 对第三方脚本、受限内容、后台入口缺少默认怀疑和清单化检查。

### 以后如何避免再次出现

- 所有访问控制一律先问：这个限制是在前端，还是在服务端？
- 任何涉及后台、受限内容、用户数据、外部脚本的改动，合并前必须做安全检查。
- 仓库默认不接受任何明文访问码、真实受限链接或后台入口信息。
- 第三方脚本默认禁止，除非给出明确必要性和回滚方案。
- 发布前安全检查写入项目规范，并且在迭代记录中留痕。

## 当前状态

- 安全整改代码已完成并通过本地语法/构建验证。
- 原第三方访客统计已恢复，CloudBase 自建统计正在影子模式并行运行。
- 待观察自建 UV 与原统计是否足够接近；确认可替代后，再移除第三方统计脚本。

## 发布后待办

1. 在 CloudBase 环境变量中确认 `APP_SESSION_SECRET`、`ADMIN_PASSWORD`、`THINGS_PERMIT_CODE`、`THINGS_CONTENT_JSON`、`ALLOWED_ORIGINS`。
2. 观察 CloudBase UV 与第三方 UV 的差异。
3. 若差异可接受，切换页面显示到自建统计，并删除第三方统计脚本。
4. 对曾经暴露过的访问码、后台密码、受限链接执行轮换。
