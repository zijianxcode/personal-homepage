{
  "name": "personal-homepage",
  "version": "1.0.0",
  "private": true,
  "description": "Static personal homepage",
  "scripts": {
    "deploy": "mkdir -p .cloudbase-deploy && cp *.html CNAME .cloudbase-deploy/ && cp -r Assets projects documents .cloudbase-deploy/ && tcb hosting deploy .cloudbase-deploy .",
    "build": "mkdir -p dist && cp *.html CNAME dist/ && cp -r Assets projects documents dist/"
  },
  "devDependencies": {
    "@cloudbase/cli": "^2.12.2"
  }
}
