{
  "name": "personal-homepage",
  "version": "1.0.0",
  "private": true,
  "description": "Static personal homepage",
  "scripts": {
    "deploy": "mkdir -p .cloudbase-deploy && cp index.html .cloudbase-deploy/ && cp -r Assets .cloudbase-deploy/ && tcb hosting deploy .cloudbase-deploy .",
    "build": "echo 'Static site, no build step.'"
  },
  "devDependencies": {
    "@cloudbase/cli": "^2.12.2"
  }
}
