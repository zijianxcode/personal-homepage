{
  "name": "personal-homepage",
  "version": "1.6.1",
  "private": true,
  "description": "Static personal homepage",
  "scripts": {
    "build:cloudbase": "rm -rf .cloudbase-deploy && mkdir -p .cloudbase-deploy && cp *.html .cloudbase-deploy/ && (test -f CNAME && cp CNAME .cloudbase-deploy/ || true) && (test -f .nojekyll && cp .nojekyll .cloudbase-deploy/ || true) && cp -r Assets projects documents academy time-ink card-freeze emergency .cloudbase-deploy/ && find .cloudbase-deploy -name '.git' -type d -exec rm -rf {} + 2>/dev/null || true && find .cloudbase-deploy/academy -name '*.md' -delete",
    "deploy": "npm run test:site-integrity && npm run build:cloudbase && node scripts/verify-deploy-bundle.js && tcb hosting deploy .cloudbase-deploy . -e homepage-1gthisc4771d43ac && bash scripts/purge-academy-markdown-from-hosting.sh",
    "deploy:emergency-status": "test -f emergency/status.json && tcb hosting deploy emergency emergency/ -e homepage-1gthisc4771d43ac",
    "health:production": "node scripts/health-check.js",
    "health:production:sync": "node scripts/health-check.js; npm run deploy:emergency-status",
    "backup:enable-github-pages": "bash scripts/enable-github-pages-backup.sh",
    "build": "npm run test:site-integrity && mkdir -p dist && cp *.html dist/ && (test -f CNAME && cp CNAME dist/ || true) && (test -f .nojekyll && cp .nojekyll dist/ || true) && cp -r Assets projects documents academy time-ink card-freeze dist/",
    "verify:production": "node scripts/verify-production.js",
    "test:site-integrity": "node check-site-integrity.js",
    "test:things-access": "node check-things-access.js"
  },
  "devDependencies": {
    "@cloudbase/cli": "^2.12.2"
  }
}
