# v1.5 移动端体验基线

发布日期：2026-05-06

## 主要更新

- 建立主站移动端体验基线，覆盖首页、研究页、Visual Coding、Experimental Project、项目详情页、`academy/` 和 `time-ink/`。
- 优化小屏响应式布局：统一 20px 移动边距，增加信息卡片间距，修复项目卡片英文断词，提升详情页标题与正文的手机端可读性。
- 强化按钮与触控体验：主要入口、返回、语言切换、GitHub / 体验按钮、许可弹窗按钮补齐可见焦点、按压反馈和 44px 以上触控区域。
- 保留原有视觉动效，同时降低移动端计算压力：粒子背景加入可见性暂停、resize 防抖、移动端 DPR 限制和后台降耗逻辑。
- 给 Visual Coding iframe 预览增加懒加载，降低移动端首屏资源竞争。
- 将后续移动端适配逻辑写入 README、CHANGELOG 和项目规范，后续新增页面按同一套响应式规则执行。

## 发布前验证

- `node --check Assets/js/script.js && node --check Assets/js/vc-page.js`
- `npm run build`
- `npm run test:things-access`

## 后续规范

以后的移动端适配默认沿用 v1.5 基线：单列优先、卡片间距舒展、按钮可触控、标题不错误断词、动效保留但要做移动端降耗。
