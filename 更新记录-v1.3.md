# v1.3 更新记录

发布日期：2026-04-08
版本标签：`v1.3`
基线提交：`42164a4`

## 本次更新

- 新增 `Experimental Project` 二级页面，并接入 `001 / Easy Presentation` 与 `002 / Time-INK` 两个三级项目页。
- `Time-INK` 已作为主站子路径项目正式接入，体验地址统一为 `https://bananabox.plus/time-ink/`，不再使用临时测试域名作为站内正式入口。
- 完成 `Time-INK` 静态托管与首屏可见性修复，解决子路径资源与首屏内容因动画初始态导致的“看似白屏”问题。
- 首页入口视觉继续打磨：补齐 `Research` 入口打字 hover、`Experimental Project` EVA 风格标题，以及 `Info` 页中英文标题切换。
- `research.html` 新增两篇论文展示项，并补充 `IEEE · SCI Q2` 与 `Frontiers in Artificial Intelligence · ESCI` 标注。
- 学术子站 `academy/` 已纳入主站部署链路，并同步至 2026-04-03 的最新内容。
- 把“主域名子路径发布”和“首屏内容默认可见”的规则正式写入项目规范与 AI 协作规则，作为后续新项目上线门槛。

## 主要更新内容

- `Experimental Project` 从占位入口升级为真实项目集合，支持二级页、三级页和项目详情阅读。
- 新增 `Easy Presentation` 案例页，完成中英双语说明、项目结构展示与 GitHub 入口。
- 新增 `Time-INK` 案例页，接入 GitHub 仓库、站内 `Try Live Demo` 入口和中英双语项目说明。
- 首页 `Info`、`Research`、`Experimental Project` 三个入口与相关内容页完成一轮内容、排版和交互整理。
- 站点的“新项目接入流程”从临时接入升级为标准化流程，后续新增项目将统一走 `bananabox.plus/<project-slug>/`。

## 发布状态

- GitHub `main` 当前基线为 `42164a4`
- 主站与 CloudBase 已完成这一轮内容同步
- 本地规范、版本记录与线上内容已统一到 `v1.3` 基线
